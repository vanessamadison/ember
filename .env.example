# Copy to .env locally (never commit .env — see .gitignore and docs/NEXT-PHASE.md).

# Phase B HTTP relay (no trailing slash). Use your machine LAN IP for device testing.
# EXPO_PUBLIC_EMBER_RELAY_URL=http://192.168.1.42:8765
